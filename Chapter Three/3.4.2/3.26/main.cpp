//3.26：在100页的二分搜索程序中，为什么用的是mid = beg + (end - beg) / 2，而非mid = (beg + end) /2;？

//beg+end超过end，值可能会是其他的
#include <iostream>

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}